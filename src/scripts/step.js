class Step {
   constructor(pitch = 50, velocity = 100, glide = false) {
      this.pitch = pitch;
      this.velocity = velocity;
      this.glide = glide;
      this.isActive = false;
   }
}






export default Step;
